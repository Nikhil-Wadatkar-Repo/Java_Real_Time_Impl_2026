server:
  port: 8083

spring:
  application:
    name: project-service
  config:
    import: optional:configserver:${CONFIG_SERVER_URL:http://localhost:8888}
  datasource:
    url: jdbc:postgresql://localhost:5432/project_db
    username: ${DB_USERNAME:project_user}
    password: ${DB_PASSWORD:project_pass}
    hikari:
      pool-name: project-hikari-pool
      maximum-pool-size: 10
      minimum-idle: 2
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
        jdbc:
          batch_size: 20
    show-sql: false
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

# Bounded timeouts only for now - see employee-service's application.yml for
# the full rationale (Resilience4j retry/circuit breaker/fallback land in Phase 9).
feign:
  client:
    config:
      employee-service:
        connect-timeout: 2000
        read-timeout: 3000

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: when-authorized

logging:
  level:
    com.enterprise.payroll.project: INFO
